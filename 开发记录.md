# 开发记录 — 方案.md

## 接口审查与实施决定

| 关联任务 | 共享边界 | 决定 |
| --- | --- | --- |
| 物理核心 / 前端 / 后端 | 初始条件、状态、模型版本 | 使用 `shared/simulation.js` 同一纯计算模块，世界坐标 +Y 向上 |
| 前端 / 后端 | 用户、发射、查询、SSE、回放、导出 | 使用方案 API；服务器为权威，客户端预览为独立 Worker |
| 调度 / 持久化 / 导出 | tick、事务、轨迹截止点 | 单计算 Worker 拥有数据库写入；导出 Worker 只读已提交截止点 |
| 原页面 / 发射控制台 | 原交互与正式卫星 | 保留原页面本地演示，新 `mission.html` 作为正式控制台，入口相互可达 |
| 所有任务 / 版本管理 | 无 Git 仓库 | 在现有项目中开发，不创建或移动用户目录，不执行提交或发布 |

Ruling: 本地开发使用 Node.js 内置 SQLite，当前 Node v24.13.0 可用但发出实验性 API 提示；避免安装额外数据库依赖，运行要求在 README 披露。

Ruling: 前端和纯物理模块为互不重叠的新文件，按明确接口分别开发；后端由主任务实现，完成后做整体审查。

## 进度

- [x] 纯物理核心及回归测试。
- [x] 后端身份、幂等发射、SQLite 档案。
- [x] 持续模拟、事务轨迹和重启恢复。
- [x] 左右联动控制台、实时观察、历史回放。
- [x] 导出与在线一致性备份，提供 Docker 部署配置（未实际部署到云端）。
- [x] 接口、恢复、浏览器与整体审查。

## 审查修正

- 正式发射前提交世界检查点，避免出生档案领先于已保存的时钟。
- 导出固定 cutoffTick、cutoffSeq、cutoffEventSeq，避免同 tick 后续事件混入。
- 单调时间推进；时钟倒退检查跨重启保留有效锚点。
- 选中较早的卫星单独订阅，不受最近100项列表限制。
- CSV 文本防公式注入，数值列保持数值含义；导出注明积分方法与误差。
- 轨迹、出生及终止事件归档；增加并发任务与内部请求队列容量保护。
- 用户反馈恢复原动态视觉：复用原吸积盘纹理、尘埃、热斑、卫星光晕和尾迹；捕获状态触发闪光与扩散光波，回放捕获同样触发。

## 已验证

- npm test：33项通过，含接口权限、恢复、检查点、导出截止一致性、在线备份还原与数值测试。
- 浏览器已观察控制台吸积盘旋转、环绕卫星发光尾迹、实际径向发射捕获及光波；无前端错误日志。

- 手机星图与历史回放捕获闪光已验证；JSON导出任务显示可下载，最新页面无错误日志。
- 恢复旧动态样式后，为实时卫星快照增加显示插值，保持发光尾迹连贯；此插值不更改后台轨迹。
- 重连获得新身份时清空旧身份缓存；身份恢复、退出和密钥轮换串行执行，避免旧响应覆盖新档案。

当前服务地址：http://127.0.0.1:4174/mission.html 。已启动本机Node服务；提供云部署配置，未向外部服务器发布。


## 科学尺度与速度（2026-09-16）

- 增加共享 `shared/units.js`，按10标称太阳质量同时标定长度、时间和速度；出生SI参数固定记录标定版本，浏览器与服务器核对版本。
- 发射参数采用km、km/s、kg，校验初速度小于光速；当前/末态与历史速度读数及导出附物理适用标记，不能把超光速近似结果当真实测量。
- 扩展对数缩放0.00001%–200%及发射范围，增加世界平面距离圈、1/2/5标尺、所选卫星定位、屏外提示、自动扩展视野。
- 正式新卫星与旧活跃卫星取消有限逃逸边界终止；旧终态原样归档并显示最后位置。控制台阴影按模型视界缩放，原吸积盘、发光尾迹和吞噬光波保留。
- CSV/JSON增加各采样点的速度大小、km/s速度分量、km距离、物理时间与标定信息；原SQLite速度向量和轨迹保留。
- 独立只读代码审查核对换算、归档、SI幂等重试及旧活跃状态恢复；修正物理时间倍率表述为2721倍慢放。
- 最终自动测试38/38通过；桌面验证百万公里定位、最小缩放参数不变、初速度光速限制、列表实时速度、捕获回放和JSON速度记录。375px手机无横向溢出，观察控件下移避免遮挡箭头，最新前端无错误日志。


## 广视野卫星亮球修复

根因：光晕至少8屏幕像素、尾迹至少0.8像素并使用lighter合成，广视野下卫星位置投影集中，固定尺寸和增亮叠加制造亮球。

修复：去除卫星和尾迹的固定最小尺寸，按投影尺度缩放；亚像素光晕按覆盖率淡出，尾迹、选中圈与名称、回放轨迹同步变暗。改为source-over合成；远景碰撞波不保留固定像素宽度，光子亮斑的阴影模糊也随尺度减弱。定位与速度数据仍可使用，后台轨迹不变。

验证：40项自动测试通过，新增视觉尺度及普通透明度合成回归；后台浏览器已检查65%正常尺度保留吸积盘和发光尾迹、0.00833%远景卫星不可分辨且没有亮球，无前端错误。用户正在填写的原预览表单未被刷新。


## 细准星与鼠标锚点缩放

- 发射准星改为细半透明虚线，中心留空；方向箭头和文字减弱以减少遮挡。
- camera.js增加视角偏移和zoomAt，滚轮围绕鼠标位置缩放；定位、重置、自动扩展处理居中恢复。卫星捕获遮罩改用屏幕坐标，防止镜头偏移后被外部裁剪误隐藏。
- 全套42项自动测试通过，包含鼠标锚点不变与偏移坐标双向转换；浏览器实测同一屏幕点在120%、200%、97.4%均回填相同(52.892550,-93.447893) km，重置返回120%，准星虚线和原黑洞动态正常，无前端错误。用户原表单保持不动。


## 黑洞常驻标识与星图拖拽

- camera.js新增pan与originMarker；黑洞固定12像素辅助标识，离开可用视野时显示方向箭头，点击保持缩放并重新居中。
- 星图使用Pointer Events、指针捕获与6像素阈值区分选点和平移；支持鼠标、单指触控和方向键，手动平移关闭自动扩展。
- 全套44项自动测试通过，新增平移投影、逆变换、平移后鼠标锚点及缩放极限/屏外标识回归。浏览器实测拖动(-250,+70)像素，标识同步移动，发射坐标和120%缩放不变；随后单击正常回填坐标。0.00001%时标识保持12像素，拖出视野出现方向提示，点击定位保留最小缩放，方向键平移40像素，无前端错误。触控通过共享指针实现，未进行实体触屏测试；用户原预览和卫星记录保持不动。


## 星图鼠标模式切换

- 增加拖拽星图/选择发射点两个互斥按钮、aria-pressed状态、模式说明与对应鼠标光标，默认选点。拖拽模式单击不改坐标；选点模式拖动既不改坐标也不平移。滚轮缩放、方向键和左侧正式发射保持可用。黑洞边缘标识避开新增工具栏。
- mission.js语法检查和7项camera测试通过。浏览器验证选点回填、切到拖拽移动(-250,+70)像素且单击不改坐标、切回选点拖动不误选、随后单击正常回填；按钮高亮和黑洞动态正常，无错误日志。测试未创建或删除卫星，用户原页面未刷新。


## 2026-09-17：独立双体质量、哈希名称与卫星跟踪

- 新发射请求显式携带two-body-pw-v1，按真实m/M使用mu_eff=mu(1+m/M)。预览、参考速度、能量和后台积分共用参数，记录/导出保留动力学版本与有效引力参数。每颗为独立相对运动实验，卫星间不耦合；旧记录保留原模型，不静默改轨迹。新质量上限10^30 kg，页面说明普通质量差异极小和有限质量PW非GR近似。
- 留空名称由服务器按创建时间戳和UUID进行SHA256，取16位生成SAT名称；同毫秒不同发射不重名，幂等重试和重启保持同名，可在发射前自定义。
- 点选画面可见卫星或列表条目开启持续视角跟踪，镜头与发光卫星使用同一插值位置，缩放比例不变；跟踪缩放围绕卫星，拖拽、方向键、定位、重置或退出按钮取消跟踪。与自动扩展视野互斥，回放和末位置也可跟踪。手机端避免按钮与速度HUD重叠。
- 51项自动测试通过：真实质量比例加速度、远场极限、质量修正圆轨道与守恒、捕获终态、旧状态恢复、名称生成/幂等/自定义/持久化、不同卫星互不影响、镜头固定目标。浏览器在独立测试数据中验证10^30 kg环绕速度80959.34 km/s与1000 kg参考78997.26 km/s、自动SAT名称与自定义名称、连续跟踪、画面点选和拖动退出，无错误日志；手机无横向溢出，HUD与按钮不重叠。
- 正式数据库已作在线完整性校验备份；服务恢复后确认原档案16颗卫星保留，停机时段继续补算。测试Cookie与本地正式服务同域时恢复原有浏览器档案访问，没有更改或删除卫星记录。
