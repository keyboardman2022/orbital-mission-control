{
  "name": "orbital-mission-control",
  "version": "1.0.0",
  "private": true,
  "description": "Persistent satellite simulation and mission control",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test physics.test.cjs camera.test.cjs tests/*.test.cjs",
    "backup": "node server/backup.js"
  }
}
